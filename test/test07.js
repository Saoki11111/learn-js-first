// 点数 0~100 が入った配列
// 配列を受け取り、最高点を返す getMax(ary) を作る

num = [];
getMax = function(numbers){
  var min=[0];
  var max=[];
  for(var count = 0; count < numbers.length; count++){}
  if(numbers[count0] < min){}
}

console.log(getMax[67, 23, 99])
